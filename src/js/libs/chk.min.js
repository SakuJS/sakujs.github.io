var chkjs = {
    'chkC': (cname) => {
        if (getCookie(cname)) {
            window.location.href = "/pages/app/dashboard/";
        }
    },
    'redir': (url) => {
        window.location.href = url;
    }
}